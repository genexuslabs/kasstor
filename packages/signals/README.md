# Kasstor Signals

A package that implements Signals to be used in any type of application. It also provides directives for using those signals in components built with the `@genexus/kasstor-core` package.

