# Vite Plugin Kasstor

A plugin for the Vite build tool to use the `@genexus/kasstor-build` and `@genexus/kasstor-insights` package in a single entry point. It also adds HMR capabilities for components built with the `@genexus/kasstor-core` package.

